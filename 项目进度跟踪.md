# STM32 电压采集 + WSL 监控项目 · 任务进度表

> 更新日期：2026-09-07
> 项目目录：E:\STM32Project\Test
> 对照方式：每次开发前/后读取本文件，勾选已完成项

---

## 第 0 步：准备（基础工程）
- [x] 打开现有工程（TIM+USART+tick 已跑通）
- [x] TIM2 定时中断（已改 500ms）翻转 LED
- [x] USART1 115200/8N1 发送 + printf 重定向
- [x] 接线：电位器两端接 3.3V / GND，中间抽头接 PA0（ADC1_IN0）
- [x] CH340 驱动 + 串口助手能收板子数据

## 第 1 周：ADC 采样跑通 ✅ 已完成（2026-09-07 验收通过）
- [x] 1.1 GPIOA 时钟 + PA0 模拟输入（Hardware/ADC.c）
- [x] 1.2 ADC1 独立模式、单次转换、12 位右对齐、校准
- [x] 1.3 规则通道 0，采样时间 55.5 周期
- [x] 1.4 TIM2 每 500ms 中断软件触发一次 ADC（Time.c ARR=5000-1）
- [x] 1.5 电压换算（纯整数 ×330/4095，无 float，MicroLIB 友好）
- [x] 1.6 串口打印 Volt=x.xxV ADC=xxxx
- [x] 1.7 ADC.c 已加入 Keil 工程，Rebuild 0 Error 0 Warning
- [x] 1.8 烧录验收：拧电位器 0.00~3.29V 平滑跟随，ADC 0~4091
  - 已知正常现象：未拧时 LED(PA1) 翻转耦合 PA0 约 ±0.05V；零点 ADC 抖动 0~4；碳膜接触小波动 → 第 2 周滑动平均消除

## 第 2 周：滤波 + 私有帧 + 命令交互（下一步）
- [x] 2.1 8 点滑动平均滤波（ADC.c 环形缓冲，Raw/Avg 对比打印）
- [x] 2.2 6 字节私有帧：AA 01 VolH VolL Stat SUM（Hardware/Protocol.c，校验和）
- [x] 2.3 串口收命令（USART RX + 接收中断 + 64字节环形缓冲）
  - [x] O → LED2 亮（用 PA2，避开 LED1 心跳翻转冲突）
  - [x] C → LED2 灭
  - [x] G → 立即采样上报一帧
- [x] 2.4 超阈报警：Stat bit0 置 1 + LED2 1Hz 闪烁（蜂鸣器太吵暂用LED代替，换蜂鸣器只改一行）
- [x] 验收：O/C 控制灯、G 立即上报、超 3.0V 报警（Stat=01，LED2闪）

## 第 3 周：Linux C 上位机（VMware + CentOS7 + FinalShell，不用 WSL）
- [x] 3.0 环境：gcc 4.8.5 / make 3.82 已装（yum 源已换阿里云 centos-vault）
- [x] 3.1 串口直通：VMware→可移动设备→CH340 连接进虚拟机，/dev/ttyUSB0 已识别（ch341 驱动），cat 见数据
- [x] 3.2 open + termios 115200/8N1/raw（serial_read.c，CentOS7 gcc4.8.5 编译通过）
- [x] 3.3 poll() 非阻塞读，先打 HEX 验证链路（帧头AA稳定/6字节/抽查校验和全对，已验收）
- [x] 3.4 帧解析：搜 0xAA 帧头 + 校验和 + 好/坏帧/失步计数（frame_parse.c，稳态坏帧=0 失步=0，已验收）
- [x] 3.5 pthread 双线程：线程A读串口解析 / 线程B每2s画条形图（monitor.c，已验收）
- [x] 3.6 Makefile + README（host/Makefile 一键编译 + host/README.md 项目文档）
- [x] 验收：终端条形图随电位器变化，拔插串口不崩

## 第 4 周（可选）：socket 转发 + GitHub
- [ ] 4.1 socket 转发 127.0.0.1:5000
- [ ] 4.2 Python matplotlib 实时曲线
- [ ] 4.3 GitHub 上传 STM32 工程 + WSL 代码 + README

---

## 当前进度快照（2026-09-07 更新）
| 模块 | 状态 | 说明 |
|---|---|---|
| TIM2 定时中断 | ✅ 完成 | 500ms（ARR=4999, PSC=7199） |
| USART1 发送 | ✅ 完成 | 115200/8N1，TX only |
| LED | ✅ 完成 | PA1/PA2 推挽 |
| ADC 采样 | ✅ 完成 | PA0=ADC1_IN0，0~3.3V 跟随正常，已验收 |
| 电压换算/打印 | ✅ 完成 | 纯整数 volt_x100=ADC*330/4095，%u.%02u 打印 |
| 串口接收 RX | ✅ 完成 | PA10 浮空输入，RXNE 中断+环形缓冲，Serial_GetByte 非阻塞取，O/C/G 命令 |
| 滑动平均滤波 | ✅ 完成 | 8 点环形缓冲 Filter_Average，启动未满时按实际个数平均 |
| 私有帧/校验 | ✅ 完成 | STM32发送 + Linux状态机解析+校验，全链路打通 |
| 命令交互/报警 | ✅ 完成 | O/C/G命令，超3.0V Stat=01+LED2闪烁，报警优先级高于命令 |
| Linux 上位机 | ✅ 完成 | 3.0-3.6 全部：termios+poll+帧解析+pthread双线程条形图+Makefile+README |

## 工程注意点（检查时发现）
1. **Serial.c 是 TX only（USART_Mode_Tx）**：第 2 周做命令交互，要先改成 Rx+Tx、开接收中断、加环形缓冲区
2. **TIM2 中断里只做采样读取**（ADC_GetValue 约几十 us），换算/打印放主循环——已按此实现
3. **电压用整数打印**（放大 100 倍），不依赖 printf %f，MicroLIB 正常；换算也是纯整数无 float
4. **LED 占用 PA1/PA2**，电位器接 PA0，不冲突；LED 翻转对 PA0 有约 0.05V 耦合，属正常
5. **函数命名避开标准库**：自定义初始化用 ADC1_Init / Timer1_Init（标准库已有 ADC_Init、TIM_TimeBaseInit）
6. **头文件自包含**：ADC.h 用 #include <stdint.h> 提供 uint16_t，避免编辑器红叉

## 自检清单（每步完成时对照）
| 检查项 | 标准 |
|---|---|
| 编译 | 0 error，warning 尽量为 0 |
| 烧录 | 正常运行不死机 |
| 串口 | 不乱码、不丢字节 |
| 中断 | 中断内不做阻塞发送/浮点运算 |
| 帧格式 | HEX 模式帧头稳定、校验和正确 |

---

## 新对话断点恢复指引（新开会话先读这里）
> **新对话第一句直接说：先读 `E:\STM32Project\Test\项目进度跟踪.md` 再继续。**

### 当前断点（2026-09-14）
- **项目主体已全部完成并验收**：第 0~2 周 STM32 硬件 + 第 3 周 Linux 上位机（3.0~3.6）全绿
- 当前可选项：
  - 读 monitor.c 代码（按 README 技术要点逐项理解）
  - 第 4 周（可选）：4.1 socket 转发 / 4.2 Python 实时曲线 / 4.3 GitHub 传仓库

### 运行环境（重要，和最初设想的 WSL 不同）
- 用 **VMware + CentOS7 + FinalShell**，不是 WSL
- CentOS 用户名 robert，家目录 ~；gcc 4.8.5 / make 3.82 已装（yum 源已换阿里云 centos-vault 7.9.2009）
- 上位机代码在 CentOS 的 `~/adc_monitor/`，Windows 备份在 `E:\STM32Project\Test\host\`
- CH340 经 VMware「可移动设备」直通为 `/dev/ttyUSB0`，**每次重新直通后要 `sudo chmod 666 /dev/ttyUSB0`**
- 编译：`gcc -Wall -o 程序名 程序名.c`；运行：`./程序名`

### 关键约定（别推翻）
- 帧 6 字节大端：AA 01 VolH VolL Stat SUM，SUM=前5字节累加低8；FRAME_LEN=6
- volt_x100=滤波ADC×330/4095；阈值 300（3.0V）
- **方案 A（用户已拍板保持）**：报警判断用原始值 Adc_Raw（快），上报电压用滤波值 Adc_Value（稳），故快速拧动时可能 Stat=01 但电压字段略滞后，属正常
- 纯整数、不用 %f（MicroLIB）；自定义初始化函数命名 ADC1_Init/Timer1_Init 避开标准库
- 蜂鸣器太吵，用 LED2(PA2) 1Hz 闪烁代替报警；LED1(PA1) 是 500ms 心跳
- 引脚：PA0=ADC1_IN0 接电位器抽头，PA9/PA10=USART1 TX/RX，电位器两端 3.3V/GND
